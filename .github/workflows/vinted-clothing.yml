name: Dan's Vault Nike Clothing Radar

on:
  push:
    paths:
      - 'vinted-radar/radar-v6.mjs'
      - 'vinted-radar/run-bot.mjs'
      - 'vinted-radar/fetch-guard.mjs'
      - 'vinted-radar/expanded-clothing-radar.mjs'
      - 'vinted-radar/clothing-discovery-hardening.mjs'
      - 'vinted-radar/clothing-discovery-selftest.mjs'
      - '.github/workflows/vinted-clothing.yml'
  schedule:
    - cron: '2-57/5 * * * *'
  workflow_dispatch:

concurrency:
  group: dans-vault-clothing-radar
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  radar:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate radar v6 and clothing discovery hardening
        run: |
          set -e
          node --check vinted-radar/fetch-guard.mjs
          node --check vinted-radar/radar-v6.mjs
          node --check vinted-radar/expanded-clothing-radar.mjs
          node --check vinted-radar/clothing-discovery-hardening.mjs
          node --check vinted-radar/run-bot.mjs
          node --check vinted-radar/radar-selftest.mjs
          node --check vinted-radar/clothing-discovery-selftest.mjs
          node vinted-radar/radar-selftest.mjs
          node vinted-radar/clothing-discovery-selftest.mjs

      - name: Validate clothing Discord webhook
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_CLOTHING_WEBHOOK_URL }}
        run: |
          test -n "$DISCORD_WEBHOOK_URL"
          curl --fail --silent --show-error "$DISCORD_WEBHOOK_URL" >/dev/null

      - name: Run clothing radar repeatedly
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_CLOTHING_WEBHOOK_URL }}
          BOT_TYPE: clothing
          STATE_NAME: clothing-state.json
        run: |
          deadline=$((SECONDS + 145))
          successes=0
          failures=0
          cycle=0
          while [ $SECONDS -lt $deadline ]; do
            cycle=$((cycle+1))
            cycle_start=$SECONDS
            echo "=== Clothing scan cycle $cycle ==="
            if node vinted-radar/run-bot.mjs; then
              successes=$((successes+1))
              node -e "const s=require('./vinted-radar/clothing-state.json'); const d=s.diagnostics||{}; console.log('Clothing v6 OK:', s.freshness?.lastScanAt, 'attempt:', s.freshness?.lastAttemptAt, 'selected:', d.successfulSearches ?? '?', '/', d.selectedSearches?.length ?? '?', 'markers:', d.catalogMarkers ?? '?', 'parsed:', d.catalogItems ?? '?', 'fresh:', d.freshItems ?? '?', 'delivered:', d.deliveredAlerts ?? '?', 'pending:', d.pendingDeliveries ?? '?', 'healthy:', d.healthyScan ?? '?')"
            else
              failures=$((failures+1))
              echo "Clothing scan cycle $cycle FAILED."
            fi
            elapsed=$((SECONDS-cycle_start))
            wait=$((30-elapsed))
            if [ $wait -gt 0 ] && [ $SECONDS -lt $deadline ]; then sleep $wait; fi
          done
          echo "Clothing cycles complete: $successes successful, $failures failed."
          if [ $successes -eq 0 ] || [ $failures -gt $successes ]; then
            echo "Clothing radar reliability threshold failed."
            exit 1
          fi

      - name: Save clothing radar state safely
        if: ${{ always() }}
        run: |
          git config user.name "Dan's Vault Clothing Radar"
          git config user.email "actions@users.noreply.github.com"
          cp vinted-radar/clothing-state.json /tmp/clothing-state.json

          for attempt in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main

            if [ -f vinted-radar/clothing-state.json ]; then
              if node -e "const fs=require('fs'); const l=JSON.parse(fs.readFileSync('/tmp/clothing-state.json','utf8')); const r=JSON.parse(fs.readFileSync('vinted-radar/clothing-state.json','utf8')); const lt=Date.parse(l.updatedAt||l.freshness?.lastAttemptAt||l.freshness?.lastScanAt||0); const rt=Date.parse(r.updatedAt||r.freshness?.lastAttemptAt||r.freshness?.lastScanAt||0); process.exit(Number.isFinite(rt)&&Number.isFinite(lt)&&rt>lt?0:1)"; then
                echo "Remote clothing state is newer; refusing stale overwrite."
                exit 0
              fi
            fi

            cp /tmp/clothing-state.json vinted-radar/clothing-state.json
            git add vinted-radar/clothing-state.json
            if git diff --cached --quiet; then exit 0; fi
            git commit -m "Update Nike clothing radar state"
            if git push origin HEAD:main; then exit 0; fi
            sleep 2
          done
          exit 1
