name: eBay Sold

on:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  ebay-sold:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run eBay sold checker
        run: node ebay-sold/run-bot.mjs
