name: eBay Sold

# Paused until the eBay checker entrypoint is restored.
# The previous scheduled workflow referenced ebay-sold/run-bot.mjs, which does not exist.
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "eBay Sold radar is paused — checker entrypoint needs rebuilding before automatic runs are re-enabled."
