name: Seller Risk

# Paused until the seller-risk entrypoint is restored.
# The previous workflow referenced seller-risk/run-bot.mjs, but that path is not present.
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Seller Risk is paused — seller-risk entrypoint needs rebuilding before automatic runs are re-enabled."
