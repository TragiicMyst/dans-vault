name: Seller Risk

on:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  seller-risk:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run seller risk bot
        run: node seller-risk/run-bot.mjs
