name: Nike Demand

on:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  demand:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run Nike demand bot
        run: node nike-demand/run-bot.mjs
