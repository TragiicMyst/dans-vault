name: Nike Demand

# Paused until the Nike demand entrypoint is restored.
# The previous workflow referenced nike-demand/run-bot.mjs, but that path is not present.
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Nike Demand is paused — demand entrypoint needs rebuilding before automatic runs are re-enabled."
