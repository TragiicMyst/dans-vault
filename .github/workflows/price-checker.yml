name: Price Checker

# Paused until the price-checker entrypoint is restored.
# The previous workflow referenced price-checker/run-bot.mjs, but that path is not present.
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Price Checker is paused — checker entrypoint needs rebuilding before automatic runs are re-enabled."
